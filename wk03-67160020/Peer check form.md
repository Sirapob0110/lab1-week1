# แบบฟอร์ม Peer Check — URI Schema

ชื่อ-นามสกุลผู้ตรวจสอบ (ผู้กรอกแบบฟอร์ม): takdanai (67160009)

ชื่อ-นามสกุลเจ้าของโปรเจกต์ที่ถูกตรวจสอบ: sirapob 67160020

| หัวข้อตรวจสอบ                                              | ผ่าน / ไม่ผ่าน | ข้อเสนอแนะ                                                                                                                                                           |
| ---------------------------------------------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| URI ใช้คำนามพหูพจน์ ไม่มีคำกริยาปน                         | ไม่ผ่าน        | พบ `/api/v1/getBooks`, `/api/v1/deleteBook/:id`, `/api/v1/createNewBook` มีคำกริยาปนอยู่ในชื่อ URI ควรเปลี่ยนเป็น `/api/v1/books` แล้วใช้ HTTP Method แยกการทำงานแทน |
| ใช้ตัวพิมพ์เล็กและขีดกลาง ไม่ใช้ camelCase หรือ underscore | ไม่ผ่าน        | `getBooks`, `deleteBook`, `createNewBook` เป็น camelCase ทั้งหมด ควรเปลี่ยนเป็นตัวพิมพ์เล็กล้วน เช่น `/books`                                                        |
| HTTP Method สอดคล้องกับการดำเนินการ                        | ไม่ผ่าน        | `GET /api/v1/deleteBook/:id` ใช้ GET ในการลบข้อมูลจริง ควรเปลี่ยนเป็น `DELETE /api/v1/books/:id` # แบบฟอร์ม Peer Check — URI Schema                                  |

ชื่อ-นามสกุลผู้ตรวจสอบ (ผู้กรอกแบบฟอร์ม): ZUNFORDZ (67160020)

ชื่อ-นามสกุลเจ้าของโปรเจกต์ที่ถูกตรวจสอบ: ............................................

| หัวข้อตรวจสอบ                                                  | ผ่าน / ไม่ผ่าน | ข้อเสนอแนะ                                                                                                                                                           |
| -------------------------------------------------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| URI ใช้คำนามพหูพจน์ ไม่มีคำกริยาปน                             | ไม่ผ่าน        | พบ `/api/v1/getBooks`, `/api/v1/deleteBook/:id`, `/api/v1/createNewBook` มีคำกริยาปนอยู่ในชื่อ URI ควรเปลี่ยนเป็น `/api/v1/books` แล้วใช้ HTTP Method แยกการทำงานแทน |
| ใช้ตัวพิมพ์เล็กและขีดกลาง ไม่ใช้ camelCase หรือ underscore     | ไม่ผ่าน        | `getBooks`, `deleteBook`, `createNewBook` เป็น camelCase ทั้งหมด ควรเปลี่ยนเป็นตัวพิมพ์เล็กล้วน เช่น `/books`                                                        |
| HTTP Method สอดคล้องกับการดำเนินการ                            | ไม่ผ่าน        | `GET /api/v1/deleteBook/:id` ใช้ GET ในการลบข้อมูลจริง ควรเปลี่ยนเป็น `DELETE /api/v1/books/:id`                                                                     |
| Status Code ครอบคลุมทั้งกรณีสำเร็จและกรณีผิดพลาด               | ไม่ผ่าน        | การสร้างข้อมูลสำเร็จควรตอบ 201 และกรณีไม่พบข้อมูลควรตอบ 404                                                                                                          |
| ความสัมพันธ์ระหว่าง Resource แสดงด้วย Nested Path อย่างเหมาะสม | ผ่าน           | โปรเจกต์นี้มี Resource เดียว (books) จึงไม่มี Nested Path ที่ต้องตรวจสอบ                                                                                             |

## ผลการทดสอบ Edge Case ผ่าน Postman# แบบฟอร์ม Peer Check — URI Schema

ชื่อ-นามสกุลผู้ตรวจสอบ (ผู้กรอกแบบฟอร์ม): ZUNFORDZ (67160020)

ชื่อ-นามสกุลเจ้าของโปรเจกต์ที่ถูกตรวจสอบ: ............................................

| หัวข้อตรวจสอบ                                                  | ผ่าน / ไม่ผ่าน | ข้อเสนอแนะ                                                                                                                                                           |
| -------------------------------------------------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| URI ใช้คำนามพหูพจน์ ไม่มีคำกริยาปน                             | ไม่ผ่าน        | พบ `/api/v1/getBooks`, `/api/v1/deleteBook/:id`, `/api/v1/createNewBook` มีคำกริยาปนอยู่ในชื่อ URI ควรเปลี่ยนเป็น `/api/v1/books` แล้วใช้ HTTP Method แยกการทำงานแทน |
| ใช้ตัวพิมพ์เล็กและขีดกลาง ไม่ใช้ camelCase หรือ underscore     | ไม่ผ่าน        | `getBooks`, `deleteBook`, `createNewBook` เป็น camelCase ทั้งหมด ควรเปลี่ยนเป็นตัวพิมพ์เล็กล้วน เช่น `/books`                                                        |
| HTTP Method สอดคล้องกับการดำเนินการ                            | ไม่ผ่าน        | `GET /api/v1/deleteBook/:id` ใช้ GET ในการลบข้อมูลจริง ควรเปลี่ยนเป็น `DELETE /api/v1/books/:id`                                                                     |
| Status Code ครอบคลุมทั้งกรณีสำเร็จและกรณีผิดพลาด               | ไม่ผ่าน        | การสร้างข้อมูลสำเร็จควรตอบ 201 และกรณีไม่พบข้อมูลควรตอบ 404                                                                                                          |
| ความสัมพันธ์ระหว่าง Resource แสดงด้วย Nested Path อย่างเหมาะสม | ผ่าน           | โปรเจกต์นี้มี Resource เดียว (books) จึงไม่มี Nested Path ที่ต้องตรวจสอบ                                                                                             |

## ผลการทดสอบ Edge Case ผ่าน Postman

| Endpoint                   | กรณีทดสอบ                     | Status Code ที่คาดหวัง | Status Code จริง | ผ่าน/ไม่ผ่าน |
| -------------------------- | ----------------------------- | ---------------------- | ---------------- | ------------ |
| GET /api/v1/books/999      | เรียกด้วย id ที่ไม่มีอยู่จริง | 404                    | 404              | ผ่าน         |
| POST /api/v1/createNewBook | ส่งข้อมูลไม่ครบ               | 400                    | 200              | ไม่ผ่าน      |
| GET /api/v1/deleteBook/999 | ลบข้อมูลที่ไม่มีอยู่จริง      | 404                    | 200              | ไม่ผ่าน      |

## ข้อเสนอแนะเพิ่มเติม

1. เปลี่ยน URI ให้เหลือเพียง Resource `books`
2. ใช้ HTTP Method ให้ถูกต้องตามมาตรฐาน RESTful
3. เพิ่ม Validation ก่อนสร้างข้อมูล
4. ปรับ Status Code ให้ถูกต้อง เช่น 201, 404 และ 409# แบบฟอร์ม Peer Check — URI Schema

ชื่อ-นามสกุลผู้ตรวจสอบ (ผู้กรอกแบบฟอร์ม): ZUNFORDZ (67160020)

ชื่อ-นามสกุลเจ้าของโปรเจกต์ที่ถูกตรวจสอบ: ............................................

| หัวข้อตรวจสอบ                                                  | ผ่าน / ไม่ผ่าน | ข้อเสนอแนะ                                                                                                                                                           |
| -------------------------------------------------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| URI ใช้คำนามพหูพจน์ ไม่มีคำกริยาปน                             | ไม่ผ่าน        | พบ `/api/v1/getBooks`, `/api/v1/deleteBook/:id`, `/api/v1/createNewBook` มีคำกริยาปนอยู่ในชื่อ URI ควรเปลี่ยนเป็น `/api/v1/books` แล้วใช้ HTTP Method แยกการทำงานแทน |
| ใช้ตัวพิมพ์เล็กและขีดกลาง ไม่ใช้ camelCase หรือ underscore     | ไม่ผ่าน        | `getBooks`, `deleteBook`, `createNewBook` เป็น camelCase ทั้งหมด ควรเปลี่ยนเป็นตัวพิมพ์เล็กล้วน เช่น `/books`                                                        |
| HTTP Method สอดคล้องกับการดำเนินการ                            | ไม่ผ่าน        | `GET /api/v1/deleteBook/:id` ใช้ GET ในการลบข้อมูลจริง ควรเปลี่ยนเป็น `DELETE /api/v1/books/:id`                                                                     |
| Status Code ครอบคลุมทั้งกรณีสำเร็จและกรณีผิดพลาด               | ไม่ผ่าน        | การสร้างข้อมูลสำเร็จควรตอบ 201 และกรณีไม่พบข้อมูลควรตอบ 404                                                                                                          |
| ความสัมพันธ์ระหว่าง Resource แสดงด้วย Nested Path อย่างเหมาะสม | ผ่าน           | โปรเจกต์นี้มี Resource เดียว (books) จึงไม่มี Nested Path ที่ต้องตรวจสอบ                                                                                             |

## ผลการทดสอบ Edge Case ผ่าน Postman

| Endpoint                   | กรณีทดสอบ                     | Status Code ที่คาดหวัง | Status Code จริง | ผ่าน/ไม่ผ่าน |
| -------------------------- | ----------------------------- | ---------------------- | ---------------- | ------------ |
| GET /api/v1/books/999      | เรียกด้วย id ที่ไม่มีอยู่จริง | 404                    | 404              | ผ่าน         |
| POST /api/v1/createNewBook | ส่งข้อมูลไม่ครบ               | 400                    | 200              | ไม่ผ่าน      |
| GET /api/v1/deleteBook/999 | ลบข้อมูลที่ไม่มีอยู่จริง      | 404                    | 200              | ไม่ผ่าน      |

## ข้อเสนอแนะเพิ่มเติม

1. เปลี่ยน URI ให้เหลือเพียง Resource `books`
2. ใช้ HTTP Method ให้ถูกต้องตามมาตรฐาน RESTful
3. เพิ่ม Validation ก่อนสร้างข้อมูล
4. ปรับ Status Code ให้ถูกต้อง เช่น 201, 404 และ 409# แบบฟอร์ม Peer Check — URI Schema

ชื่อ-นามสกุลผู้ตรวจสอบ (ผู้กรอกแบบฟอร์ม): ZUNFORDZ (67160020)

ชื่อ-นามสกุลเจ้าของโปรเจกต์ที่ถูกตรวจสอบ: ............................................

| หัวข้อตรวจสอบ | ผ่าน / ไม่ผ่าน | ข้อเสนอแนะ |
| ------------------------------------------------------------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| URI ใช้คำนามพหูพจน์ ไม่มีคำกริยาปน | ไม่ผ่าน | พบ `/api/v1/getBooks`, `/api/v1/deleteBook/:id`, `/api/v1/createNewBook` มีคำกริยาปนอยู่ในชื่อ URI ควรเปลี่ยนเป็น `/api/v1/books` แล้วใช้ HTTP Method แยกการทำงานแทน |
| ใช้ตัวพิมพ์เล็กและขีดกลาง ไม่ใช้ camelCase หรือ underscore | ไม่ผ่าน | `getBooks`, `deleteBook`, `createNewBook` เป็น camelCase ทั้งหมด ควรเปลี่ยนเป็นตัวพิมพ์เล็กล้วน เช่น `/books` |
| HTTP Method สอดคล้องกับการดำเนินการ | ไม่ผ่าน | `GET /api/v1/deleteBook/:id` ใช้ GET ในการลบข้อมูลจริง ควรเปลี่ยนเป็น `DELETE /api/v1/books/:id` |
| Status Code ครอบคลุมทั้งกรณีสำเร็จและกรณีผิดพลาด | ไม่ผ่าน | การสร้างข้อมูลสำเร็จควรตอบ 201 และกรณีไม่พบข้อมูลควรตอบ 404 |
| ความสัมพันธ์ระหว่าง Resource แสดงด้วย Nested Path อย่างเหมาะสม | ผ่าน | โปรเจกต์นี้มี Resource เดียว (books) จึงไม่มี Nested Path ที่ต้องตรวจสอบ |

## ผลการทดสอบ Edge Case ผ่าน Postman

| Endpoint | กรณีทดสอบ | Status Code ที่คาดหวัง | Status Code จริง | ผ่าน/ไม่ผ่าน |
|----------|------------|------------------------|------------------|---------------|
| GET /api/v1/books/999 | เรียกด้วย id ที่ไม่มีอยู่จริง | 404 | 404 | ผ่าน |
| POST /api/v1/createNewBook | ส่งข้อมูลไม่ครบ | 400 | 200 | ไม่ผ่าน |
| GET /api/v1/deleteBook/999 | ลบข้อมูลที่ไม่มีอยู่จริง | 404 | 200 | ไม่ผ่าน |

## ข้อเสนอแนะเพิ่มเติม

1. เปลี่ยน URI ให้เหลือเพียง Resource `books`
2. ใช้ HTTP Method ให้ถูกต้องตามมาตรฐาน RESTful
3. เพิ่ม Validation ก่อนสร้างข้อมูล
4. ปรับ Status Code ให้ถูกต้อง เช่น 201, 404 และ 409

| Endpoint                   | กรณีทดสอบ                     | Status Code ที่คาดหวัง | Status Code จริง | ผ่าน/ไม่ผ่าน |
| -------------------------- | ----------------------------- | ---------------------- | ---------------- | ------------ |
| GET /api/v1/books/999      | เรียกด้วย id ที่ไม่มีอยู่จริง | 404                    | 404              | ผ่าน         |
| POST /api/v1/createNewBook | ส่งข้อมูลไม่ครบ               | 400                    | 200              | ไม่ผ่าน      |
| GET /api/v1/deleteBook/999 | ลบข้อมูลที่ไม่มีอยู่จริง      | 404                    | 200              | ไม่ผ่าน      |

## ข้อเสนอแนะเพิ่มเติม

1. เปลี่ยน URI ให้เหลือเพียง Resource `books`
2. ใช้ HTTP Method ให้ถูกต้องตามมาตรฐาน RESTful
3. เพิ่ม Validation ก่อนสร้างข้อมูล
4. ปรับ Status Code ให้ถูกต้อง เช่น 201, 404 และ 409 |
   | Status Code ครอบคลุมทั้งกรณีสำเร็จและกรณีผิดพลาด | ไม่ผ่าน | การสร้างข้อมูลสำเร็จควรตอบ 201 และกรณีไม่พบข้อมูลควรตอบ 404 |
   | ความสัมพันธ์ระหว่าง Resource แสดงด้วย Nested Path อย่างเหมาะสม | ผ่าน | โปรเจกต์นี้มี Resource เดียว (books) จึงไม่มี Nested Path ที่ต้องตรวจสอบ |

## ผลการทดสอบ Edge Case ผ่าน Postman

| Endpoint                   | กรณีทดสอบ                     | Status Code ที่คาดหวัง | Status Code จริง | ผ่าน/ไม่ผ่าน |
| -------------------------- | ----------------------------- | ---------------------- | ---------------- | ------------ |
| GET /api/v1/books/999      | เรียกด้วย id ที่ไม่มีอยู่จริง | 404                    | 404              | ผ่าน         |
| POST /api/v1/createNewBook | ส่งข้อมูลไม่ครบ               | 400                    | 200              | ไม่ผ่าน      |
| GET /api/v1/deleteBook/999 | ลบข้อมูลที่ไม่มีอยู่จริง      | 404                    | 200              | ไม่ผ่าน      |

## ข้อเสนอแนะเพิ่มเติม

1. เปลี่ยน URI ให้เหลือเพียง Resource `books`
2. ใช้ HTTP Method ให้ถูกต้องตามมาตรฐาน RESTful
3. เพิ่ม Validation ก่อนสร้างข้อมูล
4. ปรับ Status Code ให้ถูกต้อง เช่น 201, 404 และ 409
