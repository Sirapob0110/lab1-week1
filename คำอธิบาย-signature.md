# คำอธิบาย: เหตุใด Token ที่ถูกแก้ไขจึงถูกปฏิเสธ

JWT แต่ละใบประกอบด้วย 3 ส่วน คั่นด้วยจุด (`.`) คือ **Header**, **Payload** และ **Signature** โดยสองส่วนแรก (Header, Payload) เป็นเพียงข้อมูลที่เข้ารหัสแบบ Base64URL เท่านั้น **ไม่ได้ถูกเข้ารหัสลับ (encrypt)** ใครก็ตามที่ได้ token ไปสามารถถอด Base64 อ่านเนื้อหาข้างในได้ทันที (นี่คือเหตุผลว่าทำไม payload จึงห้ามใส่ข้อมูลลับ เช่น รหัสผ่าน)

สิ่งที่ทำให้ token เชื่อถือได้ไม่ใช่การซ่อนข้อมูล แต่คือ **Signature** ส่วนที่สาม ซึ่งเซิร์ฟเวอร์สร้างขึ้นตอน `jwt.sign()` ด้วยการนำ `Header + "." + Payload` ไปเข้ารหัสแบบ HMAC ร่วมกับ `JWT_SECRET` ที่เก็บไว้ในฝั่งเซิร์ฟเวอร์เท่านั้น (ไม่เคยส่งออกไปให้ client เห็น)

เมื่อมีคำขอเข้ามาพร้อม token middleware `authenticateToken` จะเรียก `jwt.verify(token, process.env.JWT_SECRET)` ซึ่งทำงานดังนี้:

1. แยก token ออกเป็น Header, Payload, Signature
2. นำ `Header + "." + Payload` ที่ได้รับมา คำนวณ Signature ใหม่อีกครั้งด้วย `JWT_SECRET` เดียวกัน
3. เทียบ Signature ที่คำนวณใหม่ กับ Signature ที่แนบมากับ token

หากตรงกัน แปลว่า token นี้ถูกสร้างโดยเซิร์ฟเวอร์ที่ถือ secret ตัวเดียวกันจริง และ **เนื้อหาไม่ได้ถูกแก้ไขระหว่างทาง** — ถ้าไม่ตรงกัน `jwt.verify()` จะ throw error ทันที ทำให้ middleware ตอบกลับ `401 INVALID_TOKEN`

## ทำไมแก้แค่ตัวอักษรเดียวก็พังทั้งหมด

เพราะ HMAC signature ถูกคำนวณจาก**เนื้อหาทั้งหมด**ของ Header+Payload พร้อมกัน ไม่ใช่แยกทีละส่วน ถ้าเปลี่ยนแม้แต่ 1 bit ในส่วน Payload (เช่น พยายามแก้ `role: "student"` เป็น `role: "admin"` เพื่อยกระดับสิทธิ์ตัวเอง) ผลลัพธ์ของฟังก์ชัน HMAC จะเปลี่ยนไปแบบคาดเดาไม่ได้เลยทั้งหมด (คุณสมบัติของ cryptographic hash function เรียกว่า **avalanche effect**) Signature ใหม่ที่เซิร์ฟเวอร์คำนวณจากเนื้อหาที่ถูกแก้แล้ว จึงไม่มีทางตรงกับ Signature เดิมที่แนบมาใน token ได้เลย เว้นแต่ผู้แก้ไขจะรู้ค่า `JWT_SECRET` ของเซิร์ฟเวอร์ด้วย (ซึ่งไม่มีทางรู้ได้ถ้าเซิร์ฟเวอร์เก็บรักษาความลับนี้ไว้อย่างเหมาะสม)

นี่คือกลไกที่ทำให้ JWT ป้องกันการปลอมแปลงข้อมูล (tamper-proof) ได้ แม้ payload จะอ่านได้แบบ plain text ก็ตาม — ความปลอดภัยอยู่ที่ **การยืนยันความถูกต้อง (integrity)** ผ่าน Signature ไม่ใช่การซ่อนข้อมูล (confidentiality)
